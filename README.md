# EventsBot - Telegram бот для мониторинга выставок в Казахстане

Telegram-бот на aiogram 3.x для автоматического мониторинга и уведомления о B2B выставках и форумах в Казахстане.

## Технологический стек

- **Python 3.10+**
- **aiogram 3.x** - фреймворк для Telegram ботов
- **SQLAlchemy + SQLite** - база данных
- **BeautifulSoup4 + HTTPX** - парсинг веб-сайтов
- **APScheduler** - планировщик задач

## Функциональность

### Основные возможности

1. **Парсинг источников** - автоматический сбор информации о выставках с различных сайтов:
   - iteca.events
   - atakent-expo.kz
   - qazexpo.kz
   - astanahub.com/events
   - digitalbusiness.kz
   - profit.kz/events
   - worldexpo.pro
   - exposale.net
   - vystavki.su

2. **Умная фильтрация** - автоматическое исключение:
   - Мастер-классов
   - Обучения и вебинаров
   - Оставление только B2B событий (выставки, форумы, стенды)

3. **Персонализация** - пользователь выбирает:
   - Интересующие индустрии (IT, Агро, Медицина и др.)
   - Города для мониторинга
   - Опция "Выбрать все"

4. **Система обратной связи** - кнопки 👍/👎 под каждым событием:
   - 👍 - событие релевантно
   - 👎 - выбор причины отклонения:
     - "Не моя сфера"
     - "Не B2B"
     - "Малый масштаб"
   - Фидбек сохраняется и используется для улучшения рекомендаций

5. **Автоматические уведомления** - бот присылает новые события, соответствующие фильтрам пользователя

## Установка

### 1. Клонирование и переход в директорию

```bash
cd EventsBot\(Activat\)
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта (если его еще нет):

```env
API_KEY=your_telegram_bot_token_here
```

Получить токен можно у [@BotFather](https://t.me/BotFather) в Telegram.

### 5. Инициализация базы данных

База данных создастся автоматически при первом запуске бота.

## Запуск

```bash
python bot.py
```

## Структура проекта

```
EventsBot(Activat)/
├── bot.py                 # Главный файл запуска бота
├── config.py              # Конфигурация (токены, стоп-слова, списки)
├── requirements.txt       # Зависимости проекта
├── .env                   # Переменные окружения (не в git)
├── database/
│   ├── __init__.py
│   ├── models.py          # Модели БД (User, Event, Feedback)
│   └── engine.py          # Настройка подключения к БД
├── handlers/
│   ├── __init__.py
│   ├── callback_data.py   # CallbackData фабрики
│   ├── start.py           # Обработчик /start и онбординг
│   ├── settings.py        # Обработчик /settings
│   └── feedback.py        # Обработчик фидбека (👍/👎)
└── services/
    ├── __init__.py
    ├── parser.py          # Парсер сайтов выставок
    ├── scheduler.py       # Планировщик парсинга
    └── notification.py    # Отправка уведомлений пользователям
```

## Команды бота

- `/start` - начало работы, регистрация и настройка фильтров
- `/settings` - изменение настроек (индустрии и города)

## Настройка

Основные параметры можно изменить в `config.py`:

- `PARSING_INTERVAL_MINUTES` - интервал парсинга (по умолчанию 60 минут)
- `STOP_WORDS` - список стоп-слов для фильтрации
- `B2B_KEYWORDS` - ключевые слова для определения B2B событий
- `INDUSTRIES` - список доступных индустрий
- `CITIES` - список городов Казахстана

## Особенности реализации

### Парсинг

Парсер использует универсальный подход для большинства сайтов, с возможностью добавления специализированных парсеров для конкретных источников.

### Фильтрация

- Проверка на наличие стоп-слов (исключение)
- Проверка на наличие B2B ключевых слов (включение)
- Извлечение дат и городов из текста

### База данных

Используется SQLite для простоты развертывания. Модели:
- `User` - пользователи и их фильтры
- `Event` - события/выставки
- `Feedback` - обратная связь пользователей
- `UserEvent` - связь пользователей и отправленных событий

## Разработка

Для добавления новых источников парсинга:

1. Добавьте URL в `PARSING_SOURCES` в `config.py`
2. При необходимости создайте специализированный метод парсинга в `services/parser.py`
3. Универсальный парсер `parse_generic_site()` обработает большинство сайтов

## Лицензия

Проект создан для мониторинга выставок в Казахстане.

## Поддержка

При возникновении проблем проверьте:
1. Правильность токена в `.env`
2. Наличие всех зависимостей (`pip install -r requirements.txt`)
3. Логи в консоли при запуске бота
